# Custom viewer usage

This demo features a folder tree with a viewer that allows version comparison and custom element properties.
Can be based on both existing Autodesk hub and Buckets.
When comparing two model versions, a list of differences is created and can be later pulled into Revit by an API call from a script.
